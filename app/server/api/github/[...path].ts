// Hides the token (runtimeConfig.githubToken) from the client

export default defineEventHandler(async (_event) => {
  // TODO: forward request to api.github.com with token and handle rate limiting
})
