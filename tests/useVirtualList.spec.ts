import { describe, it, expect } from 'vitest'

describe('useVirtualList', () => {
  it.todo('renders only items within the visible window + buffer')
  it.todo('updates the visible window on scroll')
})
